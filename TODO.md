# HereBlingy
- scan for ores to collect above and below the player too in strip mining mode
- avoid removing boxes in both modes if there are any in the area selected or in the path of strip mining.
- if player changes mode, and then manually selects to "start" again, make sure to stop the previous mode first so that we correctly start with the new task (i had a terraforming auto stopped for having completed the area and wouldnt let me start strip mining)
